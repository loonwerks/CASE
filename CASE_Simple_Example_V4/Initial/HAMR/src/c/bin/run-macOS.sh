#!/usr/bin/env bash
#
# This file is autogenerated.  Do not edit
#
set -e
export SCRIPT_HOME=$( cd "$( dirname "$0" )" &> /dev/null && pwd )
cd $SCRIPT_HOME

# Uncomment the following to prevent terminal from closing if app crashes
#PREVENT_CLOSE="; bash -i"

open -a Terminal "macOS-build/RadioDriver_Impl_SW_Radio_App${PREVENT_CLOSE}" &
open -a Terminal "macOS-build/FlightPlanner_Impl_SW_FlightPlanner_App${PREVENT_CLOSE}" &
open -a Terminal "macOS-build/FlightController_Impl_SW_FlightController_App${PREVENT_CLOSE}" &
read -p "Press enter to initialise components ..."
macOS-build/Main
read -p "Press enter again to start ..."
macOS-build/Main