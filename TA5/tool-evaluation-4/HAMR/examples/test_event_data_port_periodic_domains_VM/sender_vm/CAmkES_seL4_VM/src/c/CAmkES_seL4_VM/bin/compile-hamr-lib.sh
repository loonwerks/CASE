#!/usr/bin/env bash
#
# This file is autogenerated.  Do not edit
#
set -e
export SCRIPT_HOME=$( cd "$( dirname "$0" )" &> /dev/null && pwd )
export PROJECT_HOME=$( cd "$( dirname "$0" )/.." &> /dev/null && pwd )

cd "${PROJECT_HOME}/hamr/top_impl_Instance_src_process_src_thread"
mkdir -p sel4-build
cd sel4-build
cmake -DCMAKE_C_COMPILER=aarch64-linux-gnu-gcc ..
make $MAKE_ARGS


cd "${PROJECT_HOME}/hamr/top_impl_Instance_dst_process_dst_thread"
mkdir -p sel4-build
cd sel4-build
cmake -DCMAKE_C_COMPILER=aarch64-linux-gnu-gcc ..
make $MAKE_ARGS


cd "${PROJECT_HOME}/hamr/SlangTypeLibrary"
mkdir -p sel4-build
cd sel4-build
cmake -DCMAKE_C_COMPILER=aarch64-linux-gnu-gcc ..
make $MAKE_ARGS

