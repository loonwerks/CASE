#!/usr/bin/env bash
#
# This file is autogenerated.  Do not edit
#
set -e
export SCRIPT_HOME=$( cd "$( dirname "$0" )" &> /dev/null && pwd )
cd $SCRIPT_HOME

# Uncomment the following to prevent terminal from closing if app crashes
#PREVENT_CLOSE="; bash -i"

x-terminal-emulator -T producer_t_i_producer_producer_App -e sh -c "linux-build/producer_t_i_producer_producer_App${PREVENT_CLOSE}" &
x-terminal-emulator -T consumer_t_i_consumer_consumer_App -e sh -c "linux-build/consumer_t_i_consumer_consumer_App${PREVENT_CLOSE}" &
read -p "Press enter to initialise components ..."
linux-build/Main
read -p "Press enter again to start ..."
linux-build/Main