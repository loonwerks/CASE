#!/usr/bin/env bash
#
# This file is autogenerated.  Do not edit
#
set -e
export SCRIPT_HOME=$( cd "$( dirname "$0" )" &> /dev/null && pwd )
cd $SCRIPT_HOME

# Uncomment the following to prevent terminal from closing if app crashes
#PREVENT_CLOSE="; bash -i"

cygstart mintty /bin/bash "cygwin-build/producer_t_producer_producer_App.exe${PREVENT_CLOSE}" &
cygstart mintty /bin/bash "cygwin-build/filter_t_filter_filter_App.exe${PREVENT_CLOSE}" &
cygstart mintty /bin/bash "cygwin-build/consumer_t_consumer_consumer_App.exe${PREVENT_CLOSE}" &
read -p "Press enter to initialise components ..."
cygwin-build/Main.exe
read -p "Press enter again to start ..."
cygwin-build/Main.exe