#!/usr/bin/env bash
#
# This file is autogenerated.  Do not edit
#
set -e
export SCRIPT_HOME=$( cd "$( dirname "$0" )" &> /dev/null && pwd )
cd $SCRIPT_HOME

# Uncomment the following to prevent terminal from closing if app crashes
#PREVENT_CLOSE="; bash -i"

open -a Terminal "macOS-build/UxAS_thr_Impl_uxas_UxAS_thread_App${PREVENT_CLOSE}" &
open -a Terminal "macOS-build/RadioDriver_thr_Impl_radio_RadioDriver_App${PREVENT_CLOSE}" &
open -a Terminal "macOS-build/CASE_AttestationGate_thr_Impl_am_gate_CASE_AttestationGate_App${PREVENT_CLOSE}" &
read -p "Press enter to initialise components ..."
macOS-build/Main
read -p "Press enter again to start ..."
macOS-build/Main